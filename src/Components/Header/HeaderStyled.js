import { Link, styled } from "@mui/material";

export const StyledLink = styled(Link)({
  textDecoration: "none",
  color: "inherit",
  fontSize: { sm: 18, md: 24 },
  margin: 0,
  height: "auto",
});
